spring.application.name=user-service
spring.config.import=optional:classpath:error-messages.yml
server.port=8081

spring.datasource.url=jdbc:mysql://${DB_HOST}:${DB_PORT}/${DB_NAME}?serverTimezone=${TZ}&useUnicode=true&characterEncoding=${DB_ENCODING}
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.jpa.hibernate.ddl-auto=none

# spring.data.redis.host=${REDIS_URL:localhost}
# spring.data.redis.port=${REDIS_PORT:6379}
# spring.data.redis.database=${REDIS_DATA:0}
# spring.data.redis.timeout=${REDIS_DATA:60000}

minio.url=${MINIO_URL:http://localhost:9000}
minio.access-key=${MINIO_ACCESS_KEY:minio}
minio.secret-key=${MINIO_SECRET_KEY:minio123}
# minio.bucket=${MINIO_BUCKET:product-img}

se.app.jwtSecret= =======================Spring===========================
se.app.jwtExpirationMs=86400000
se.app.jwtRefreshExpirationMs=7878787878

logging.level.com.se.user_service=DEBUG
logging.level.org.springframework.cache=DEBUG
